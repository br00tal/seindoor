This is the default config file for seindoor, a python script to play audio files when a reed (door) sensor is opened.  The original intent was to play the Seinfeld bass riff whenever the door was opened, but any audio file can technically be played.
